apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app-prod
  namespace: my-app-prod
spec:
  ingressClassName: nginx
  rules:
    - host: tegarr.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-app
                port:
                  number: 80
